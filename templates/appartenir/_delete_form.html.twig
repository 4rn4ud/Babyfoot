<form method="post" action="{{ path('app_appartenir_delete', {'id': appartenir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appartenir.id) }}">
    <button class="btn">Delete</button>
</form>
